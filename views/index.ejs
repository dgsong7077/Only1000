<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sentence Learning App</title>
  <link rel="stylesheet" href="/style.css">  <!-- 정적 파일 (CSS) -->
</head>
<body>
  <h1>문장 추가하기</h1>

  <form action="/sentences" method="POST">
    <div>
      <label>문장:</label>
      <input type="text" name="sentence" placeholder="여기에 문장을 입력하세요">
    </div>

    <div>
      <label>구성된 단어:</label>
      <input type="text" name="keywords" placeholder="중요한 단어 입력 (쉼표로 구분)">
    </div>

    <div>
      <label>문장을 사용하는 상황:</label>
      <textarea name="situation" placeholder="이 문장을 사용할 상황을 설명하세요"></textarea>
    </div>

    <button type="submit">문장 추가하기</button>
    <button type="reset">취소</button>
  </form>

  <h2>미리 보기</h2>
  <% if (sentence) { %>
    <p><strong>문장:</strong> <%= sentence %></p>
    <p><strong>구성된 단어:</strong> <%= keywords.join(", ") %></p>
    <p><strong>상황:</strong> <%= situation %></p>
  <% } %>
</body>
</html>
